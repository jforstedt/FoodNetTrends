// Run from a temporary directory with an explicit --outdir and -work-dir.
process.executor = 'local'
process.clusterOptions = ''
process.penv = null
env.R_LIBS = System.getenv('R_LIBS') ?: ''

params.max_memory = '2.GB'
executor.submitRateLimit = '100/sec'
executor.queueSize = 4
